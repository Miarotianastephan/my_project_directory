
<nav class="navbar navbar-header navbar-header-transparent navbar-expand-lg border-bottom">
    <div class="container-fluid">


        <ul class="navbar-nav topbar-nav ms-md-auto align-items-center">
            <li class="nav-item topbar-user dropdown hidden-caret">
                <a class="dropdown-toggle profile-pic" data-bs-toggle="dropdown" href="#" aria-expanded="false">
                    <div class="avatar-sm">
                    <img src="{{ asset("/img/welcome2.png") }}" alt="..." class="avatar-img rounded-circle" />
                    </div>
                    <span class="profile-username">
                        <span class="op-7">Bonjour </span>
                        {% if app.user %}
                            <span class="fw-bold">{{app.user.userMatricule}} !</span>
                        {% else %}
                            <span class="fw-bold">!</span>
                        {% endif %}
                    </span>
                </a>
                <ul class="dropdown-menu dropdown-user animated fadeIn">
                    <div class="dropdown-user-scroll scrollbar-outer">
                    {% if app.user %}
                        <li>
                            <div class="user-box">
                                <div class="avatar-lg">
                                    <img src="{{ asset("img/welcome2.png") }}" alt="image profile" class="avatar-img rounded" />
                                </div>
                                <div class="u-text">
                                    <h4>Matricule: {{app.user.userMatricule}}</h4>
                                    
                                </div>
                            </div>
                        </li>
                        <li>
                            <div class="dropdown-divider"></div>
                                <p class="text-muted" style="padding: 0.75% 0.75% 0.75% 5%">Groupe: {{app.user.GroupUtilisateur.grpLibelle}}</p>
                            <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="{{ path("user_logout") }}">Se deconnecter</a>
                        </li>
                    {% endif %}
                    </div>
                </ul>
            </li>
        </ul>
    </div>
</nav>