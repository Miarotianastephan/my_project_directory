{#
{{ form_start(form, { 'attr': { 'id': 'formulaire', 'data-cancel': path('app_banque_index') } }) }}

    {{ form_widget(form) }}

#}

{{ form_start(form) }}

<div class="card-body col-12 d-flex flex-column gap-2">
    <div class="form-group">
        {{ form_label(form.nom_banque) }}
        {{ form_widget(form.nom_banque, {'attr': {'class': 'form-control'}}) }}
    </div>
</div>
<div class="card-footer py-3 px-5 d-grid gap-2 d-md-flex justify-content-md-end">
    <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
</div>
{{ form_end(form) }}
