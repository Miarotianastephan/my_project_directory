{% extends 'base.html.twig' %}

{% block title %}TransactionType{% endblock %}

{% block body %}
    <h1>TransactionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction_type.id }}</td>
            </tr>
            <tr>
                <th>Trs_code</th>
                <td>{{ transaction_type.trsCode }}</td>
            </tr>
            <tr>
                <th>Trs_definition</th>
                <td>{{ transaction_type.trsDefinition }}</td>
            </tr>
            <tr>
                <th>Trs_libelle</th>
                <td>{{ transaction_type.trsLibelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transaction_type_index') }}">back to list</a>

    <a href="{{ path('app_transaction_type_edit', {'id': transaction_type.id}) }}">edit</a>

    {{ include('transaction_type/_delete_form.html.twig') }}
{% endblock %}
