{% extends 'base.html.twig' %}

{% block title %}BFM Application de comptabilité en intranet{% endblock %}
{% block page_title %}Section Admin{% endblock %}
{% block page_subtitle %}
    {# Enregistrement de demande de fonds #}
{% endblock %}

{% block page_options %}
    {{ include('chequier/_delete_form.html.twig') }}
{% endblock %}
{% block page_main_content %}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <div class="card-title">Modifier les informations de la banque</div>
            </div>
            <div class="card-body col-12 px-md-5 d-flex flex-row">
                <div class="col-1">
                    <button title="Revenir à la liste"
                            style="border: none;background-color: transparent; text-align: start">
                        <a href="{{ path('app_chequier_index') }}">
                            {{ ux_icon('heroicons:arrow-left-circle', {height: '60%', width:'75%',color:'#2a2f5b'}) }}
                        </a>
                    </button>
                </div>
                <div class="col px-3 d-flex flex-column gap-3">
                    {{ include('chequier/_form.html.twig', {'button_label': 'Modifier'}) }}
                </div>
            </div>
        </div>
    </div>
</div>

{#
{{ include('chequier/_form.html.twig', {'button_label': 'Update'}) }}

<a href="{{ path('app_chequier_index') }}">back to list</a>

{{ include('chequier/_delete_form.html.twig') }}
#}

{% endblock %}
